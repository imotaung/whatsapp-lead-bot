# WhatsApp Lead Bot

Capture WhatsApp leads via Twilio, store in Google Sheets, auto-reply with Calendly.

## Setup

1. Copy `.env.example` to `.env` and fill credentials.
2. Run locally: `uvicorn app.main:app --reload`
3. Deploy to Render using `render.yaml`.

See full instructions in original documentation.